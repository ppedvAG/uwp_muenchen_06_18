<UserControl
    x:Class="BooksToDo.Controls.DateTimePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BooksToDo.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Border BorderBrush="Black" BorderThickness="5" Padding="5">
        <StackPanel Orientation="Horizontal">
            <DatePicker Margin="10" Date="{x:Bind DatePickerValue,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TimePicker Margin="10" Time="{x:Bind TimePickerValue,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Border>
</UserControl>
